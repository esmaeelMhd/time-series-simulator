name: rssm_small
type: latent_ssm
# Alias knobs for CLI convenience (Hydra): model.dim_h=...
dim_h: null
dim_z: null
hidden_dim: 96
latent_dim: 16
encoder_dim: 48
decoder_layers: 2
num_layers: 1
dropout: 0.0
min_scale: 0.05
