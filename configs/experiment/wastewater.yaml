# @package _global_

defaults:
  - /dataset: wastewater
  - /model: rssm_wastewater
  - /model_defaults: default
  - /training: wastewater_rssm
  - /serving: wastewater
  - _self_

output:
  runs_dir: runs
  run_name: full_categorical

misc:
  device: cuda
  seed: 42
  deterministic: false

tracking:
  backend: none
  project: timesim-wastewater
  run_name: null
  tags: [rssm, wastewater]
  log_config: true
