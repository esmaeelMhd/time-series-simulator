base_config: configs/wastewater.yaml

# Arbitrary deep overrides merged onto base_config after load.
overrides: {}

# Optional CLI passthrough to scripts/train.py
models: null
epochs: null
steps_per_epoch: null
device: null
