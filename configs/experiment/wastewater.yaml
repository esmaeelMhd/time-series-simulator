# @package _global_

defaults:
  - /dataset: wastewater
  - /model: rssm_wastewater
  - /training: wastewater_rssm
  - /serving: wastewater
  - _self_

architecture:
  primary_model: latent_ssm
  legacy_model_types: [lstm, dlinear, nlinear, tft, transformer, xgboost]
  migration_phase: rssm_primary

output:
  runs_dir: runs
  run_name: full_with_time

misc:
  device: cuda
  seed: 42
  deterministic: false

tracking:
  backend: none
  project: timesim-wastewater
  run_name: null
  tags: [rssm, wastewater]
  log_config: true

